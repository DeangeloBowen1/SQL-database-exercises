-- Database usage tutorial
SHOW DATABASES;
USE elo_db; -- USE database_name;
SELECT database(); -- similar to pwd on a terminal (also a function)
SHOW CREATE DATABASE elo_db; -- Shows how the database was created

-- Part 2: Alternatively you can use the following:
-- use quotes_db;
-- show tables;
-- show databases; to show another query (However the top result will on be shown)